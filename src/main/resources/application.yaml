spring:
  application:
    name: P1_Monew
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never

logging:
  level:
    root: info

monew:
  rss:
    enabled: true
    cron: "0 0 * * * *"
    hankyung:
      url: "https://www.hankyung.com/feed/all-news"