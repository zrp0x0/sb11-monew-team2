server:
  port: ${PORT:8080}

spring:
  application:
    name: P1_Monew
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never

logging:
  level:
    root: info